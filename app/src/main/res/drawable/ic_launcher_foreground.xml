<?xml version="1.0" encoding="utf-8"?>
<!--
  The Whisper mark: three slanted bars with rounded ends, staggered down and to
  the right. Drawn as round-capped strokes rather than filled outlines so the
  caps are exactly circular and the geometry stays legible/editable.

  Authored to sit inside the adaptive icon safe zone: a 108 viewport with the
  art kept within roughly 24..84 once the 11-unit stroke's round caps are
  accounted for.

  This is a faithful reconstruction from the raster brand pack, not the vector
  master. Replace with the real SVG-derived paths when those exist.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:pathData="M31,43 L73,33"
        android:strokeColor="#FFFFFF"
        android:strokeWidth="11"
        android:strokeLineCap="round" />
    <path
        android:pathData="M33,59 L75,49"
        android:strokeColor="#FFFFFF"
        android:strokeWidth="11"
        android:strokeLineCap="round" />
    <path
        android:pathData="M35,75 L77,65"
        android:strokeColor="#FFFFFF"
        android:strokeWidth="11"
        android:strokeLineCap="round" />
</vector>
