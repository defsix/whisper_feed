<?xml version="1.0" encoding="utf-8"?>
<!--
    Nothing leaves through the platform, by either route.

    cloud-backup would put the reading database and subscription list in the
    reader's Google Drive without ever asking. device-transfer is a direct
    phone-to-phone copy with no server in between, which is far more
    defensible — but it is still data leaving the phone without being chosen,
    and the app now has its own restore for exactly that case. One sentence
    that is true beats two with an exception in them.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
