# Java class files
*.class

# Generated files
.project
.classpath
.project.properties
bin/
build/
gen/
out/
.idea/
.kotlin/

# Gradle files
.gradle/
google-gsa/build/
app/build/
release/
neo/

# Local SDK config (machine-specific, must not be committed)
local.properties

# Release signing. The keystore itself lives outside the repository; this
# file only points at it, and still holds its passwords.
keystore.properties
*.jks
!app/debug.keystore
